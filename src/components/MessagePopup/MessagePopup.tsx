import styles from './MessagePopup.module.css';

const MessagePop = () => {
  return (
    <div className={styles.MessagePop}>
      <h1>Message Title</h1>
      <p>Message info...</p>
    </div>
  );
}

export default MessagePop;
